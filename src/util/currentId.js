export default class CurrentId {  constructor() {    // ======== > singleton < ======== //    if (CurrentId.exists) {      return CurrentId.instance;    }    CurrentId.instance = this;    CurrentId.exists = true;    // ======== > singleton < ======== //  }  _currentId = null  get currentId() {    return this._currentId  }  set currentId(id) {    this._currentId = id;  }}