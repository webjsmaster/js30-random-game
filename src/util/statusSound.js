import soundClick from './../public/click.mp3'import soundLoss from './../public/loss.mp3'import soundWin from './../public/win.mp3'export default class StatusSound {  constructor() {    // ======== > singleton < ======== //    if (StatusSound.exists) {      return StatusSound.instance;    }    StatusSound.instance = this;    StatusSound.exists = true;    // ======== > singleton < ======== //  }  _statusSound = false;  get statusSound() {    return this._statusSound  }  set statusSound(status) {    this._statusSound = status;  }  playSound(type) {    if (type === 'click') {      const music = new Audio(soundClick);      music.play();    } else if (type === 'loss') {      const music = new Audio(soundLoss);      music.play();    } else if (type === 'win') {      const music = new Audio(soundWin);      music.play();    }  }}